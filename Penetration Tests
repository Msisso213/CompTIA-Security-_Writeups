-Physical: modify the boot process, boot from other media, modify or replace OS files
-Passive reconnaissance is using the informations outside the organization ex social media, corporate website, forums, reddit, etc.
-Active reconnaissance is using information inside the network ex nmap, DNS queries, OS scans, etc.
